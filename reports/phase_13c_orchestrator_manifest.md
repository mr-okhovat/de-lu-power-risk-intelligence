# Phase 13C — Orchestrator manifest integration

Added artifact manifest generation to the project checkpoint runner.

When screenshot refresh is enabled, the manifest runs after screenshot capture so image hashes are current.
