# Data availability index

The data availability index shows which datasets are present, registered, adapted, contract-checked and analytics-ready.

It is built from:

- dataset adapter registry
- intake summary
- processed data directory

The index is a control layer. It does not validate trading logic or signal quality.
