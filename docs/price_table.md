# Price table

The price table is built from the validated SMARD price source.

Current source:

- filter: 4169
- region: DE-LU
- resolution: hour

The table is kept separate from risk signals until the price layer passes its own quality checks.
